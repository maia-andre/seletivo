function soma (x, y) {
    return x + y;
};

const multiplicao = function(x, y){
    return x * y;
};

const dividir = (x, y) => {
    return x / y;
};

const objeto = () => ({nome: 'ANDRÉ', sobrenome: 'MAIA' });